mutation RemoveExercise($id: ID!, $name: String!, $description: String!, $tags: [ID!]) {
    removeExercise(input: {id: $id, name: $name, description: $description, tags: {sync: $tags}}) {
        id
    }
}
