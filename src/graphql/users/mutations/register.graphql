mutation RegisterMutation($email: String!, $name: String!, $password: String!) {
    register(input: {email: $email, name: $name, password: $password, password_confirmation: $password}) {
        tokens {
            access_token
        }
    }
}
